//! Ember+ viewer desktop application.

// Public-facing API consumed by the (not-yet-wired) GUI front-end.
#[allow(dead_code)]
mod address_book;

fn main() {
    println!("emberviewer — scaffold");
}
